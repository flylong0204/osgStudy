# tutorial
SET(EXAMPLE_NAME 11aNodeFollower)
SET(EXAMPLE_FILES 
	TestNodeFollower.cpp
	../../Util/Axis.h
	)
START_EXAMPLE()
IF(MSVC)
	SET_TARGET_PROPERTIES(${EXAMPLE_NAME} PROPERTIES FOLDER "tutorial")
ENDIF()
