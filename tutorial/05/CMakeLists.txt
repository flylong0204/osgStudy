# tutorial
SET(EXAMPLE_NAME 05LoadModel)
SET(EXAMPLE_FILES 
	TestTutorial.cpp
	../../Util/Axis.h
	)
START_EXAMPLE()
IF(MSVC)
	SET_TARGET_PROPERTIES(${EXAMPLE_NAME} PROPERTIES FOLDER "tutorial")
ENDIF()
