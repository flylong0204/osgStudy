# cegui 测试工程
FIND_PATH(CEGUI_INCLUDE_DIR CEGUI.h)
FIND_PATH(CEGUI_LIB_DIR CEGUIBase-0_d.lib)
INCLUDE_DIRECTORIES(${CEGUI_INCLUDE_DIR})
LINK_DIRECTORIES(${CEGUI_LIB_DIR})
FIND_PATH(GLEW_INCLUDE_DIR CEGUI.h)
FIND_PATH(GLEW_LIB_DIR CEGUIBase-0_d.lib)
INCLUDE_DIRECTORIES(${GLEW_INCLUDE_DIR})
LINK_DIRECTORIES(${GLEW_LIB_DIR})


SET(EXAMPLE_NAME ceguidemo1)
SET(EXAMPLE_FILES 
	main.cpp
	)
START_EXAMPLE()
TARGET_LINK_LIBRARIES(${EXAMPLE_NAME} 
	debug opengl32
	debug glew_d
	debug CEGUIBase-0_d.lib
	debug CEGUIOpenGLRenderer-0_d
	debug CEGUILuaScriptModule-0_d
)

IF(MSVC)
	SET_TARGET_PROPERTIES(${EXAMPLE_NAME} PROPERTIES FOLDER "3rdParty")
ENDIF()